[package]
name = "mosaic-fs-demo"
version = "0.2.0"
entry = "server.am"
description = "Filesystem-routed demo with public/ static auto-pairing (amalgame-web v0.13.0)."

[dependencies]
amalgame-web      = ">=0.13.0"
amalgame-net-http = ">=0.9.6"
